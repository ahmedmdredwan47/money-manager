import { DashboardView } from "@/features/dashboard/components/dashboard-view";

export const metadata = {
  title: "Dashboard | WealthWise Money Manager",
};

export default function DashboardPage() {
  return <DashboardView />;
}
