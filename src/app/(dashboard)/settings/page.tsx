import { SettingsView } from "@/features/settings/components/settings-view";

export const metadata = {
  title: "Settings | WealthWise Money Manager",
};

export default function SettingsPage() {
  return <SettingsView />;
}
