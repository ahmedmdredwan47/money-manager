import { ProfileView } from "@/features/profile/components/profile-view";

export const metadata = {
  title: "Profile | WealthWise Money Manager",
};

export default function ProfilePage() {
  return <ProfileView />;
}
