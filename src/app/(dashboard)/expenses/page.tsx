import { ExpensesView } from "@/features/expenses/components/expenses-view";

export const metadata = {
  title: "Expenses | WealthWise Money Manager",
};

export default function ExpensesPage() {
  return <ExpensesView />;
}
